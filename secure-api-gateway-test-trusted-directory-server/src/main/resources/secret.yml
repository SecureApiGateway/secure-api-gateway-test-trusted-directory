trustedDirectory:
  signing:
    keystorePwd: ${TTD_SIGNING_KEYSTORE_PWD}
    keystoreKeyPwd: ${TTD_SIGNING_KEYSTORE_KEY_PWD}
  ca:
    keystorePwd: ${TTD_CA_KEYSTORE_PWD}
    keystoreKeyPwd: ${TTD_CA_KEYSTORE_KEY_PWD}
